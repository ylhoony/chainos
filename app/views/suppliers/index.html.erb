<%= render partial: "section" %>

<script id="suppliers-index-template" type="text/x-handlebars-template">
  <table>
    <tr class="table-header">
      <th>Supplier Name</th>
      <th>Payment Term</th>
      <th>Currency</th>
      <th>Status</th>
      <th></th>
      <th></th>
    </tr>
    {{#each suppliers}}
      <tr class="table-content">
        <td class="td-center">{{name}}</td>
        <td class="td-center">{{payment_term.name}}</td>
        <td class="td-center">{{currency.name}}</td>
        <td class="td-center">
        {{#if status}}
          <input type="checkbox" checked disabled>
        {{else}}
          <input type="checkbox" disabled>
        {{/if}}
        </td>
        <td class="td-center"><a href="/suppliers/{{id}}">View</a></td>
        <td class="td-center"><a href="/suppliers/{{id}}/edit">Edit</a></td>
      </tr>
    {{/each}}
  </table>
</script>